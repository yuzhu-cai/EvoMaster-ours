apiVersion: batch/v1
kind: Job
metadata:
  name: ml-master-metaworld-mt10
  namespace: default
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: metaworld-train
        # ★ 这里改成你私有仓库里的镜像 ★
        image: 10.254.40.242:30550/metaworld_rl:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 0
          runAsGroup: 0
        env:
        - name: NVIDIA_DRIVER_CAPABILITIES
          value: "compute,utility,graphics"

        - name: TASK_CONFIG
          value: "mt10"             

        - name: TRAIN_TIMEOUT
          value: "7200"     

        resources:
          limits:
            nvidia.com/gpu: 1
            cpu: "4"
            memory: "80Gi"
          requests:
            nvidia.com/gpu: 1
            cpu: "4"
            memory: "40Gi"

        volumeMounts:
          - name: dshm
            mountPath: /dev/shm

        command: ["/bin/bash", "-lc"]
        args:
        - >
          cd /workspace && source /opt/conda/etc/profile.d/conda.sh && conda activate metaworld && 
          bash train.sh && bash eval.sh


      volumes:



      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 64Gi
