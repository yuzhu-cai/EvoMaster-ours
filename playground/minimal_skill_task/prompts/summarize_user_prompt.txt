# Instruction 全文
{task_description}

# 多轮检索结果（来自 Search Agent）
{search_results}

# nodes_data 路径（用于获取选定节点的 prepare_code）
nodes_data: {nodes_data}

你需要选择一个最符合 Instruction 全文的任务，将任务名称与 `prepare_code` 写入 PDF 文件。PDF 须保存在当前工作目录。