# Instruction 全文
{task_description}

# 多轮检索结果
{search_results}

# nodes_data 路径（用于获取选定节点的 data_knowledge / model_knowledge）
nodes_data: {nodes_data}

如果有多组候选则比较多组候选（node_id、distance、content），选出与 instruction 最匹配的任务，提取该任务的 data_knowledge 与 model_knowledge，并保存为 PDF 文件。完成后使用 finish。
如果没有候选，则直接建立说明失败原因的pdf文件。完成后使用 finish。