# 任务
学习库中 summary 格式，并输出**数据库结构说明**与**关于 query 的严格总结说明**。**不输出完整 query 句子，由 Plan 根据你的说明来写 query。**

# Instruction 全文
{task_description}

# 数据库路径
vec_dir: {vec_dir}
nodes_data: {nodes_data}
model: {model}

# nodes.jsonl（绝对路径 — 仅查看前 5 行）
仅用 str_replace_editor 的 view 查看该路径。**不得调用 use_skill（如 rag 的 get_info/run_script）**。不要读 config.yaml 或去别处查找。完成后用 finish 结束。
{nodes_jsonl_path}

请按以下结构输出（仅两部分，**不要**写「本任务专用 query」或任何完整 query 句）：

**(1) 数据库结构说明**
- nodes.jsonl 中与检索相关的字段及含义。
- 前几条记录的示例内容与长度（可摘录片段）。

**(2) query 写作规范（严格总结说明）**
- 与库中 summary 同风格：句式、长度、用词习惯（写清具体要求，便于 Plan 严格遵循）。
- 禁止项：关键词/短语、照抄某条 summary、过短或风格不符的句子。
- 可给符合/不符合规范的示例对比。
- **只给规范与说明，不要写出针对当前 instruction 的一条完整 query。** Plan 将根据本说明和 instruction 自行输出 query 进行检索尝试。
