# 当前阶段输入（含 Analyze 输出：仅 (1) 数据库结构说明 与 (2) query 写作规范）
{stage_input}

# Instruction 全文
{task_description}

# 数据库参数
vec_dir: {vec_dir}
nodes_data: {nodes_data}
model: {model}

**重要：** Analyze 只给出了「(2) query 写作规范」的严格总结说明，**没有**给出完整 query。你需要**根据 (2) 的规范说明和上方 instruction**，自己写出一条符合规范的 query 进行检索尝试，不得随意发挥。

- **首轮：** 根据 stage_input 中的 (2) query 写作规范（句式、长度、风格、禁止项）和 instruction 全文，写一条**符合规范的完整英文 query**，并设定 top_k、threshold。输出格式：query: <你写出的完整句> top_k: <数字> threshold: <数字>。
- **检索后（第二轮等）：** 若上一轮**无结果或结果过少**，第二轮**必须放宽 threshold**（建议 1.5～2.0 或更高，不要沿用 0.5 等过严值），否则可能仍无结果。query 仍须符合 Analyze 的 (2) 规范（可沿用首轮 query）；可调整 top_k、threshold。
- 输出格式：query: <符合 Analyze (2) 规范的完整句> top_k: <数字> threshold: <数字>。
