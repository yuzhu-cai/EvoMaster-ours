# Plan Agent 输出（包含本轮参数与多轮检索策略）
{plan_output}

# 当前轮的初始检索参数（可在多轮策略指导下进行后续调整）

query: {query}
top_k: {top_k}
threshold: {threshold}
vec_dir: {vec_dir}
nodes_data: {nodes_data}
embedding_type: {embedding_type}
embedding_dimensions: {embedding_dimensions}

